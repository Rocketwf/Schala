import { FullProfile, BasicProfile } from '../models/profile/Profile'

export class ProfileFactory {

    build -b s0s31p10p90
    authorId: string):  FullProfile[] {
        return {} as FullProfile[]
    }
}
